# fetcher-github

The JOBS `github` fetcher as a standalone, JOBS-buildable repo (consumed by the JOBS fetcher manifest, built offline with vendored deps + the seeded Go toolchain). See architecture/bootstrap.md.

```
jobs develop --source .
```
